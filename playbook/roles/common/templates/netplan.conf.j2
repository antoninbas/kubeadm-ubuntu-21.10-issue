---
network:
  version: 2
  renderer: networkd
  ethernets:
    # this assumes that the private network created by Vagrant uses this inteface name
    enp0s8:
      addresses:
      - {{ node_ipv4 }}/24
